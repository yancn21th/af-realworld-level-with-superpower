meta {
  name: Delete comment
  type: http
  seq: 6
}

delete {
  url: {{host}}/api/articles/{{slug}}/comments/{{comment_id}}
  body: none
  auth: none
}

headers {
  Authorization: Token {{token}}
}

assert {
  res.status: eq 204
}

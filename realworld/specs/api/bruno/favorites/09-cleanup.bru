meta {
  name: Cleanup
  type: http
  seq: 9
}

delete {
  url: {{host}}/api/articles/{{slug}}
  body: none
  auth: none
}

headers {
  Authorization: Token {{token}}
}

assert {
  res.status: eq 204
}
